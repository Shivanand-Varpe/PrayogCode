#include <stdio.h>

int main(void) {
    printf("Entering infinite loop for timeout testing...\n");
    fflush(stdout);

    while (1) {
        // Continuous loop to verify execution timeout and container termination
    }

    return 0;
}

